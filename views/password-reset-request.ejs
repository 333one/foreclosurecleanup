<!DOCTYPE html>
<html lang="en">
<head>
    <title>Password Reset Request</title>
    <%- include('includes/head-boilerplate.ejs') %>
</head>
<body>

    <%- include('includes/narrowScreen-top-boilerplate.ejs') %>

                <h3 class="headlineLarge -centerAlign -bottomMarginMedium">Password Reset</h3>

                <p class="textLarge -bottomMarginMedium">Please enter the email associated with your account.</p>

                <form action="/password-reset-request" method="POST">

                    <div class="input__unit -bottomMarginLarge"> 
                        <label for="email" id="targetEmailLabel" class="input__label
                            <% if (typeof emailError != "undefined") { %>
                                -errorColor
                            <% } %>               
                            ">Email Address:</label>
                        <input id="email" class="input__field
                            <% if (typeof emailError != "undefined") { %>
                                -errorBorder
                            <% } %>
                                " name="email" <%= emailAttributes %> value="<%= userInput.email %>" autofocus>
                        <% if (typeof emailError != "undefined") { %>
                        <div id="targetEmailErrorMessage" class="input__errorMessage"><%= emailError %></div>
                        <% } %> 
                    </div>

                    <div class="-bottomMarginLarge">
                        <button id="narrowScreen__button">Request Reset</button>
                    </div> 

                </form> 

    <%- include('includes/narrowScreen-bottom-boilerplate.ejs') %>

</body>
</html>