<!DOCTYPE html>
<html lang="en">
<head>
    <title>Delete Your Account</title>
    <%- include('includes/head-boilerplate.ejs') %>
</head>
<body>

    <%- include('includes/narrowScreen-top-boilerplate.ejs') %>

                <h3 class="headlineLarge -centerAlign -bottomMarginMedium">Delete Your Account</h3>
                <p class="textLarge -bottomMarginMedium">If you would like to delete your account fill in your password below and click "Delete Account"</p>

                <form action="/delete-your-account" method="POST">

                    <div class="input__unit -bottomMarginLarge"> 
                        <label for="password" class="input__label
                            <% if (typeof deleteAccountError != "undefined") { %>
                                -errorColor
                            <% } %>               
                            ">Password:</label>
                        <div class="password__container 
                        <% if (typeof deleteAccountError != "undefined") { %>
                            -errorBorder
                        <% } %>
                        ">
                            <input id="password" class="input__passwordField" name="password" <%= passwordAttributes %>>
                            <div class="password__toggle"><img id="password__eye" src="images/eye-slash-solid.svg" alt="show / hide password"></div>
                        </div>
                        <% if (typeof deleteAccountError != "undefined") { %>
                        <div class="input__errorMessage"><%= deleteAccountError %></div>
                        <% } %> 
                    </div>

                    <div class="-bottomMarginLarge">
                        <button id="narrowScreen__button">Delete Account</button>
                    </div> 

                </form>

    <%- include('includes/narrowScreen-bottom-boilerplate.ejs') %>

</body>
</html>